# Feature development flow

Route intent through `ask-to`, sharpen it with `grilling`, publish a concrete
spec with `to-spec`, split it with `to-tickets`, implement the slices, and use
`review-fix-loop` before `ship-subissue`. The Skill Lab graph can show the
dependencies involved in the flow.
