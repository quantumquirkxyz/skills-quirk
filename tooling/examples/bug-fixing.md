# Bug fixing flow

Use `diagnosing-bugs` to reproduce and localize the failure, `tdd` to encode
the regression, and `implement` to make the smallest fix. Run
`contribution-workflow-optimizer` before opening the pull request and preserve
the reproduction and test evidence in the handoff.
